# Launch GO checklist — 2026-06-15

Decision: flip the growth switch now (24 human recruiter replies = real signal;
interviews follow). The product foundation is built, deployed, and verified —
what remains is **manual channel execution on your accounts**.

## Foundation — built + live (verified)

| Piece | Status |
|---|---|
| Programmatic SEO (sitemap 102 URLs, robots, Schema.org, meta) | ✅ live, `seo_health` gates CI |
| `{competitor} alternative` pages (LazyApply refunds, Sonara hallucinations) | ✅ live, JSON-LD |
| Public proof page `/proof` (live DB numbers) | ✅ live |
| Landing social-proof band (auto-shows ≥200 apps) | ✅ **open** (249 sent) |
| Referral: 1 free month of Pro when a friend gets a year of Pro | ✅ live |
| Interview-Conversion Engine + funnel telemetry | ✅ live |

## Honest numbers for copy (update before each post)

As of 2026-06-15: **249 applications submitted · 37 confirmed by employer ATS ·
24 human recruiter replies.** Pull fresh anytime:

```bash
npx tsx scripts/funnel_report.ts 30          # or the /proof page (live)
```

**FTC guardrail:** use sent / confirmed / replies only. **Do NOT claim
interviews** (we have 0) or invent testimonials. Positioning = "every
application is one you can actually win — eligibility-checked, ATS-verified,
tailored." Replies "in days, not months" is supportable; interviews are not.

## Ordered go-live actions (manual — your accounts)

1. **Google Search Console** — add property `resumeai-bot.ru`, verify, submit
   `https://resumeai-bot.ru/sitemap.xml`. (Indexing is the gate for all SEO
   traffic — do this first.) See `docs/marketing/seo-runbook.md`.
2. **Directories** — submit to the list in
   `docs/marketing/outreach-and-directories.md` (AI tool directories, SaaS
   listings). CAC≈0, compounding backlinks.
3. **Reddit / forums** — value-first posts per `docs/marketing/reddit-and-forums.md`
   (respect each sub's self-promo rules; lead with the eligibility-gate insight,
   not the pitch).
4. **Product Hunt** — launch using `docs/marketing/product-hunt-launch.md`
   (tagline, description, first comment, asset list). Pick a Tue–Thu.
5. **Build-in-public** — post the real funnel numbers per
   `docs/marketing/build-in-public.md` — the honest "249 sent / 24 replies / 0
   interviews yet, here's what we're fixing" narrative is itself shareable.
6. **Referral** — tell existing/free users: share your link, when a friend gets a
   year of Pro you get a free month (`/dashboard/referrals`).

## After traffic starts

- Watch `/admin/pmf` week-over-week trends (signups, conversions, replies,
  interviews) — the first interview is the proof point that unlocks paid spend.
- Keep auto-apply quality high (eligibility gate + fit ≥50); volume is supply-
  bound, not the bottleneck. More eligible companies > more keywords.
