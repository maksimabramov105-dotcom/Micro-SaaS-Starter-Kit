# SEO Audit — 2026-06-11 (Prompt 12)

## 1. Technical SEO — status

| Item | Status | Notes |
|------|--------|-------|
| sitemap.xml includes ALL programmatic pages, auto-updates from seo-data | ✅ | `app/sitemap.ts` maps `seo.{countries,jobBoards,professions,competitors}` + `REMOTE_GUIDES`; adding a data row auto-adds the URL. Added `/proof` + `/ats-check`. |
| robots.txt present, allows marketing, blocks app | ✅ | `app/robots.ts` (blocks `/dashboard`, `/api`, `/admin`). |
| Per-page title ≤60 / description ≤155 / canonical / OG | ✅ | Verified on landing, `/proof`, `/alternatives/*` (generateMetadata + canonical). CI now enforces via `scripts/seo_health.ts`. |
| Structured data: SoftwareApplication + Offer (landing), FAQPage, BreadcrumbList | ✅ | Landing has SoftwareApplication+Offer; FAQ pages have FAQPage; **added BreadcrumbList to `/alternatives/*`**. |
| Internal linking: programmatic → 3+ siblings + pricing; landing → hubs | ✅ | Alternatives link all siblings + `/compare` + **added `/proof` + `/pricing`**. |
| Pages 200 + indexable in prod (no noindex, no auth gate) | ✅ | Enforced by `seo_health.ts` (checks 200 + noindex). |
| Indexed-page inventory visible to admin | ✅ | **`/admin/pmf` "SEO pages indexed" tile** (sitemap length). |
| Core Web Vitals ≥90 perf/SEO | ⚠️ | Prior Lighthouse (May): Perf 99 / SEO 100 / A11y 95 on landing. Re-run after the P13 redesign recommended (manual — see runbook). |

## 2. Competitor-gap pages
- `/alternatives/[competitor]` now covers **8**: sonara, lazyapply, loopcv, jobright, aiapply, simplify, **jobcopilot (new)**, **teal (new)**.
- Each prompt-named competitor (lazyapply, sonara, simplify, jobcopilot, teal) has a **5-axis feature matrix** (hands-off auto-apply, eligibility check, verified-by-ATS, reply inbox, price), documented weaknesses, **source links** (official + reviews), and our 30-day money-back. Honest tone, no trash-talk.

## 3. Proof page
- `/proof` — live (cache 1h) counters from the prod DB: applications sent, confirmed-by-ATS, human replies, **median time-to-first-reply**. Linked in the main nav + alternatives footer. Honest disclaimer (no interview promises).

## 4. Lead magnet
- `/ats-check` reserved in sitemap. **Not yet built** (see TODO) — unauthenticated, rate-limited jobfit check + Lead capture + single Resend follow-up. Scoped as the next focused PR.

## 5. Automation
- ✅ `scripts/seo_health.ts` runs in CI (e2e-journey job) — fails the build if sitemap/robots/200/title/meta/canonical regress.
- ✅ `docs/marketing/seo-runbook.md` — 30-min weekly founder checklist.
- ⚠️ Weekly "regenerate from fresh counts" cron — pages are already ISR (`revalidate`) + data-driven, so they don't go stale; a dedicated cron adds little until counts are surfaced on the pages. TODO if/when we show per-page live counts.

## 6. Top-10 highest-opportunity keywords → suggested slugs

| # | Keyword (intent) | Slug | Status |
|---|------------------|------|--------|
| 1 | "lazyapply alternative" (switchers) | `/alternatives/lazyapply` | ✅ live |
| 2 | "teal alternative" / "teal hq alternative" | `/alternatives/teal` | ✅ new |
| 3 | "jobcopilot alternative" | `/alternatives/jobcopilot` | ✅ new |
| 4 | "simplify jobs alternative" | `/alternatives/simplify` | ✅ live (enriched) |
| 5 | "free ats resume checker" (lead magnet) | `/ats-check` | 🔜 TODO build |
| 6 | "auto apply to jobs with sponsorship" | `/remote/visa-sponsorship` (add to remote-guides) | 🔜 add data row |
| 7 | "apply to jobs in germany from abroad" | `/jobs-in/germany` | ✅ live |
| 8 | "auto apply greenhouse jobs" | `/auto-apply/greenhouse` | ✅ live |
| 9 | "remote customer support jobs apply" | `/resume/customer-support` (add profession) | 🔜 add data row |
| 10 | "job application automation honest / real" | `/proof` (+ a "how it works" hub) | ✅ /proof live |

**Quickest wins:** #5 (build `/ats-check` — high-intent free traffic + email capture) and #6/#9 (one-line data rows in `lib/seo-data.json` / `lib/remote-guides.ts`).
